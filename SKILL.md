---
name: sky-take-out-skill
description: 设计或扩展 Spring Boot 餐饮/订单后台中的用户与管理端边界、订单流程、运营报表和多模块 Maven 结构。适用于有 JWT、MyBatis、缓存、支付或对象存储集成的业务系统。
---

# Spring Boot Transactional Business Backend

将餐饮订单系统提炼为可复用的业务后台模式：模块边界、角色隔离、订单状态、运营指标和外部集成均保持可测试、可审计。

## 适用范围

- Spring Boot 多模块业务后台，包含共享组件、DTO/实体模块和服务端模块。
- 同时面向用户端与管理端，且使用不同权限、接口和操作语义。
- 有商品、购物车、订单、支付、报表、文件或消息通知等典型交易流程。

不要把此 Skill 当作固定的表结构或接口清单。它帮助设计边界和约束，领域模型应随实际业务调整。

## 模块与职责

1. `common`：通用结果、异常、配置属性、基础工具和横切能力；不要放具体订单规则。
2. `pojo`：实体、DTO、VO 与页面/报表契约；区分入参与出参，避免控制器直接暴露持久化实体。
3. `server`：鉴权拦截、控制器、应用服务、事务、Mapper 与外部适配的组合。

## 实施顺序

1. 先为用户端和管理端分别写出可访问资源与操作；认证身份不等于拥有所有资源权限。
2. 将订单状态迁移集中在服务层，并为每一次迁移规定前置状态、幂等行为与失败结果。
3. 使用 DTO 接收请求，服务层校验日期范围、金额、库存和状态；数据库查询参数必须限定。
4. 报表服务返回结构化指标与时间范围；导出由专门服务写响应，并处理内容类型、文件名和失败情形。
5. 支付、对象存储、Redis、WebSocket 等外部能力均使用配置属性、超时和可测试适配层。

## 复用资料

- 阅读 [架构提炼](references/architecture.md) 了解多模块交易后台的职责。
- 阅读 [运营报表契约](references/reporting-contract.md) 了解统计与导出的设计检查点。
- [ClosedDateRange.java](examples/java/ClosedDateRange.java) 是可复用的日期范围值对象；它不替代时区、营业日或数据库时钟策略。

## 边界说明

本仓库只包含对本地餐饮业务项目的原创工程总结和独立示例，不含原项目源码、配置、模板、密钥或数据。
